version: 2.1
jobs:
  build: 
    machine: 
      image: ubuntu-1604:202007-01 # VM will run Ubuntu 16.04 for this release date
    steps:
      - run: sudo apt-get install tree -y
      - run: tree . -a 
      - run: source ../src/gradle_setup.bash && install_java && install_gradle
      - run: source ../tests/test_gradle_setup.bats
